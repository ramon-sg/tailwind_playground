{
  "name": "TODO",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "parcel:dev": "parcel src/*.html --port 4000",
    "parcel:build": "parcel build src/*.html",
    "clean": "rimraf dist .cache",
    "dev": "npm-run-all -s clean parcel:dev",
    "build": "npm-run-all -s clean parcel:build"    
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.4.1",
    "postcss": "^8.4.12",
    "posthtml-expressions": "^1.9.0",
    "posthtml-include": "^1.7.3",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.0.24"
  }
}
